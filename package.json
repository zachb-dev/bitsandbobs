{
  "devDependencies": {
    "sass": "^1.86.0"
  },
  "scripts": {
    "dev": "powershell -ExecutionPolicy Bypass -File dev.ps1 -Message \"$env:COMMIT\"",
    "deploy": "powershell -ExecutionPolicy Bypass -File deploy.ps1",
    "serve": "bundle exec jekyll serve --config _config.yml,_config.dev.yml"

  }
}
